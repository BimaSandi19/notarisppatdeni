# Production Environment Configuration
# Sevalla Deployment - Kantor Notaris Deni Nugraha

# Application
APP_NAME=WebsiteDN
APP_ENV=production
APP_KEY=base64:ldltM/+B6N8MRQyfGzcJr6KTaOSfOYeIePtesR8YOKs=
APP_DEBUG=false
APP_TIMEZONE=Asia/Jakarta
APP_URL=https://your-domain.sevalla.app

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

# Logging
LOG_CHANNEL=stack
LOG_STACK=single,daily
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error
LOG_STDERR_FORMATTER=null

# Database Configuration (Update with Sevalla credentials)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=sevalla_database_name
DB_USERNAME=sevalla_database_user
DB_PASSWORD=sevalla_database_password
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# Queue Configuration
DB_QUEUE_CONNECTION=database
DB_QUEUE_TABLE=jobs
DB_QUEUE=default
DB_QUEUE_RETRY_AFTER=90
QUEUE_CONNECTION=database
QUEUE_FAILED_DRIVER=database-uuids

# Session Configuration
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_CONNECTION=null
SESSION_STORE=null
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax

# Cache Configuration
CACHE_DRIVER=file
CACHE_PREFIX=websitedn_cache

# Redis Configuration (if using Redis)
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=deninugrahakantornotaris@gmail.com
MAIL_PASSWORD=pzhuyitlrscuwgci
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=deninugrahakantornotaris@gmail.com
MAIL_FROM_NAME="Kantor Notaris Deni Nugraha"
MAIL_URL=null

# AWS Configuration (if needed)
AWS_ACCESS_KEY_ID=null
AWS_SECRET_ACCESS_KEY=null
SQS_SUFFIX=null
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=null
AWS_USE_PATH_STYLE_ENDPOINT=false

# Cache Configuration
DB_CACHE_CONNECTION=mysql
DB_CACHE_LOCK_CONNECTION=mysql
DB_CACHE_LOCK_TABLE=cache_locks

# Memcached Configuration
MEMCACHED_HOST=127.0.0.1

# Database URL Configuration
DB_URL=null
MYSQL_ATTR_SSL_CA=null

# Security Headers
SECURE_HEADERS=true

# Telescope (Disable in production for performance)
TELESCOPE_ENABLED=false

# Broadcasting (if needed)
BROADCAST_CONNECTION=log

# Filesystem
FILESYSTEM_DISK=local

# Additional Production Settings
DEBUGBAR_ENABLED=false
ASSET_URL=null
MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
